{
  "name": "coworker",
  "version": "1.0.0",
  "description": "Local desktop AI coding assistant MVP",
  "main": "electron/main.js",
  "scripts": {
    "desktop": "electron .",
    "build:frontend": "npm --prefix frontend run build",
    "build:pybackend": "cd backend && rm -rf dist build && /Users/leon/Documents/CodeProjects/coworker/backend/venv/bin/python -m PyInstaller --clean --noconfirm pybackend.spec 2>&1 | tail -20 && cd ..",
    "build:windows": "npm run build:frontend && npm run build:pybackend && npx electron-builder --win --config electron-builder.config.json",
    "build:macos": "npm run build:frontend && npm run build:pybackend && npx electron-builder --mac --config electron-builder.config.json",
    "build:macos-universal": "npm run build:frontend && npm run build:pybackend && npx electron-builder --mac universal --config electron-builder.config.json",
    "build:linux": "npm run build:frontend && npm run build:pybackend && npx electron-builder --linux --config electron-builder.config.json",
    "build": "npm run build:frontend && npm run build:pybackend && npx electron-builder --mac universal --config electron-builder.config.json"
  },
  "keywords": [
    "electron",
    "ai-agent",
    "coding-assistant",
    "langgraph",
    "fastapi",
    "copilot",
    "desktop-app"
  ],
  "author": "leonjackman <544084263@qq.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/leonjackman/coworker.git"
  },
  "devDependencies": {
    "electron": "43.2.0",
    "electron-builder": "26.15.3"
  },
  "dependencies": {
    "electron-updater": "6.8.9",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  }
}
